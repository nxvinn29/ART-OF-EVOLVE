# 🧪 Tests

This directory contains unit and widget tests for the **Art of Evolve** application.

## 📂 Structure

- `src/`: Mirrors the `lib/src/` structure.
- `helpers/`: Mocks, fakes, and test utilities.

## 🚀 Running Tests

Run all tests:
```bash
flutter test
```

Run with coverage:
```bash
flutter test --coverage
```
